services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      NODE_ENV: production
      DB_PATH: /app/data/cms.db
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
